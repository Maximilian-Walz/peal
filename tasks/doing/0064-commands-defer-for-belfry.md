---
plan: required
---

# 0064 — A defer command for Belfry: `tasks.commands.defer`

## Intent

Belfry's `task_defer` tool, on a `commands` project, can now run a contract command that makes one task depend on another (Belfry #155, `tasks.commands.defer`, with `{task}` and `{on}` as one shell word each, run on a worker in a synced clone). Peal provides that command, so a session under Belfry that finds its task blocked on another records the dependency in Peal's storage the same way `/peal:defer` would.

## Scope

- A CLI entry (for example `peal depend {task} {on}`) that adds `{on}` to `{task}`'s `depends`, straight into the storage like `/peal:revise`, refusing a cycle (#26's check) and a task that has a claim other than the caller's.
- `peal init --stage belfry` writes `defer:` into the `.belfry.yml` it generates, and Peal's own `.belfry.yml` gets it.
- The `filed: <id>` line Belfry reads from a filing job is task 0044's; this task only checks the two fit together.

## Done when

- Harnesses cover the new command on both storages, its refusal of a cycle, and the `belfry` stage writing `defer:`.

## Raw

From an idea a Belfry session filed while building Belfry #155 (PR #171).

## Notes

2026-09-26: Proposed plan (NOT agreed yet). The questions to the human timed out twice
(Belfry's approve tool hit the 1800s MCP idle timeout), so the plan waits for their answers.

- #26's cycle check (`peal_cycle_check_text`) is on main already, so this task does not
  depend on 0055. The two touch the same files, so expect small merge conflicts.
- The command is `peal depend ID ON`. It is allowed on a free or blocked task, or on a
  claim held in this clone (`claimed-live wt:` of this repository). It refuses:
  - a remote claim, or a label-only claim,
  - a task awaiting merge or done,
  - an unknown `ON`, or `ON` equal to `ID`,
  - a depends cycle.
- It writes straight into the storage:
  - Files: main's backlog copy, through `peal_push_main`, with a dated Notes line and the
    subject `docs(tasks): defer NNNN <slug>, waits for MMMM [NNNN]`. The pre-push gate
    already allows that subject.
  - Issues: a `Depends on #N` line in the body, plus a comment.
- An `ON` already in `depends` exits 0 and writes nothing.
- `init --stage belfry` adds `defer: .peal/peal depend {task} {on}` to the `commands`
  block. Peal's own `.belfry.yml` and the contract section of `docs/design.md` get it too.
- Touches: `plugin/bin/peal`, `plugin/lib/backlog.sh`, `plugin/lib/backlog.test.sh`,
  `plugin/lib/store*.sh`, `plugin/lib/init.sh`, `plugin/lib/init.test.sh`, `.belfry.yml`,
  `docs/design.md`.
- Size M; model and merge default.

Open questions, each with the planner's default:

1. Caller: any claim held in this clone counts as the caller's, parked local claims
   included.
2. Files: write main's backlog copy while the claim lives, and document the risk of the
   close pull request conflicting.
3. An `ON` still only on an open main-write pull request is refused, with a hint to
   retry; add a note for 0044.
4. Reuse the gate's `defer` subject; the gate does not change.
5. Issues: leave the `in progress` label as it is.
6. The belfry stage writes `defer:` for files storage only. On issues storage the
   github-issues backend defers natively.
7. A `.belfry.yml` generated by an older Peal becomes "not Peal's": accept, and say so in
   the Outcome.
8. Notes line: `<date>: waits for <on> (deferred by its session)`, with no reason argument.
9. `ON` takes task ids only.
10. A repeated call succeeds and writes nothing.
11. Update `docs/design.md` even though it is outside Scope.


---

## Outcome

